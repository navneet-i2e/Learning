str = "Navneet Anand is a coder";

//string also caleed character array
//contains various built in methods
s
console.log(str,str.length);
for(let i=0;i<str.length;i++)
{
 console.log(str[i])
}

for(let ele of str)
{
    console.log(ele)
}
//for each does not work in strings

